.side_bar
  .user-name
    .user-name__box
      .user-name__box__name
        = current_user.name
      .user-name__box__icon
        = link_to new_group_path, class: "user-name__box__icon__btn__edit" do
          = icon('far', 'edit')
        = link_to edit_user_path(current_user), class: "user-name__box__icon__btn__cog" do
          = icon('fas', 'cog')
  .group-list
    - current_user.groups.each do |group|
      .group
        = link_to '#' do
          .group__name
            = group.name
          .group__message
            メッセージはまだありません。
    - current_user.groups.each do |group|
      .group
        = link_to '#' do
          .group__name
            = group.name
          .group__message
            メッセージはまだありません。
    - current_user.groups.each do |group|
      .group
        = link_to '#' do
          .group__name
            = group.name
          .group__message
            メッセージはまだありません。
